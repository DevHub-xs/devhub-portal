apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devhub-portal-cert-ingress
  namespace: devhub-cert
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - cert.devhub.example.com
    secretName: devhub-portal-cert-tls
  rules:
  - host: cert.devhub.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devhub-portal-cert-service
            port:
              number: 80

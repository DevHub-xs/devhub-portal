name: CI DevHub Portal

on:
  push:
    branches: [feature/*]
  pull_request:
    branches: [main, development, feature/*]

jobs:
  ci:
    name: Integration
    uses: DevHub-xs/devhub-actions/.github/workflows/portal-ci.yml@main
    with:
      node-version: '20.x'
